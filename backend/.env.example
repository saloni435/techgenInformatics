# Email Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Server Configuration
PORT=5000

# Frontend URLs (for CORS)
FRONTEND_URL=http://localhost:5173

# Instructions:
# 1. Replace 'your-email@gmail.com' with your actual Gmail address
# 2. For EMAIL_PASS:
#    - Go to Google Account settings
#    - Enable 2-Factor Authentication
#    - Generate an "App Password" for this application
#    - Use that 16-character app password here (not your regular password)
# 3. Save this file as .env in the backend folder
# 4. Add .env to your .gitignore file to keep credentials secure